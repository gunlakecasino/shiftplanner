/**
 * ShiftBuilder UI version (sheet footer).
 * Bump for releases. Use semver (e.g. 1.1).
 */
export const SHIFTBUILDER_VERSION = "1.210";

export function shiftBuilderVersionLabel(): string {
  return `v${SHIFTBUILDER_VERSION}`;
}