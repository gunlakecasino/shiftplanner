/**
 * Do not replace the live board with a loading shell on Settings↔canvas↔Team.
 * The layout hold keeps the outgoing paint; incoming views mark ready in place.
 */
export default function ShiftBuilderLoading() {
  return null;
}
