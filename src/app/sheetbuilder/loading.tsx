/**
 * Soft-nav between /sheetbuilder surfaces must not swap in a skeleton.
 * Route hold + in-place ready marker keep the outgoing paint until the next view is up.
 */
export default function SheetBuilderLoading() {
  return null;
}
